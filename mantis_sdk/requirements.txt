# runtime deps for the rest surface. for browser automation also install playwright
# (pip install "mantis_sdk[browser]") and run: python -m playwright install chromium
requests>=2.31
pandas>=2.0
websockets>=10.4
